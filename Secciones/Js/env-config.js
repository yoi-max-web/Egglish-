/* ============================================================
   ENV-CONFIG.JS
   ─────────────────────────────────────────────────────────────
   Ya NO debe contener GEMINI_API_KEY. La key ahora vive
   únicamente en el servidor (Vercel → Environment Variables →
   GEMINI_API_KEY), leída por /api/gemini.js.
   Puedes eliminar el <script src="/Secciones/Js/env-config.js">
   de tus HTML si no lo usas para nada más; se deja este archivo
   vacío por compatibilidad para no romper páginas que aún lo
   referencian.
   ============================================================ */